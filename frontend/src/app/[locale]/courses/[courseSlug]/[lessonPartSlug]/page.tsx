

interface Props {
  params: {
    lessonPartSlug: string
  }
}

export default function LessonPart({ params }: Props){
     const { lessonPartSlug } = params
    return(
        <div>

        </div>
    )
}